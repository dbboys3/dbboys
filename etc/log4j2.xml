<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">

    <!-- 日志目录变量 -->
    <Properties>
        <Property name="LOG_DIR">logs</Property>
    </Properties>

    <Appenders>

        <!-- ================= 控制台输出 ================= -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout charset="UTF-8" pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1}:%L - %msg%n"/>
        </Console>

        <!-- ================= 文件滚动输出 ================= -->
        <RollingFile name="RollingFile"
                     fileName="${LOG_DIR}/dbboys.log"
                     filePattern="${LOG_DIR}/dbboys-%d{yyyy-MM-dd}-%i.log.gz">

            <PatternLayout  pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1}:%L - %msg%n"/>

            <Policies>
                <!-- 按天滚动 -->
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <!-- 单文件超过 10MB 滚动 -->
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>

            <!-- 最多保留 30 个文件 -->
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>

    </Appenders>

    <Loggers>

        <!-- 根日志：同时输出到控制台和文件 -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Root>

    </Loggers>

</Configuration>
